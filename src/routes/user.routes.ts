import { Router } from 'express';

const usersRouter = Router();

// routes
// usersRouter.get('/', );

// usersRouter.post('/', );

// usersRouter.put('/', );

// usersRouter.post('/auth', );

export default usersRouter;
